<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="phonedetailMapper">
    <!--启用二级缓存-->
    <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

    <select id="showallphonedetails" parameterType="cn.edu.aust.entity.PhoneDetail"
            resultType="cn.edu.aust.entity.PhoneDetail">
        SELECT * from phonedetails p
    </select>


    <insert id="add_phones" parameterType="cn.edu.aust.entity.PhoneDetail">
        INSERT phonedetails
        ( model, brand,os, imei, uuid, zoomsuppported, videostabilization, focusmode, flashmode, previewsize, details)
        VALUES
        (#{model},#{brand},#{os},#{imei},#{uuid},#{zoomsuppported},#{videostabilization},#{focusmode},#{flashmode},#{previewsize},#{details})


    </insert>

</mapper>